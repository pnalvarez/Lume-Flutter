/// Learning category shown during onboarding and preferences.
class CategoryDomain {
  const CategoryDomain({
    required this.id,
    required this.name,
  });

  final int id;
  final String name;
}
